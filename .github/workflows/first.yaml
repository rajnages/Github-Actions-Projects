name: First Workflow

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        run: echo "This is the first step"

      - name: Multiple Commands
        run: |
          node -v
          npm -v
